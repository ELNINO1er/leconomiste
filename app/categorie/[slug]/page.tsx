import Link from "next/link"; import {articles,categories} from "../../../lib/mock-data"; import {Header} from "../../components/Header"; import {ArticleCard} from "../../components/ArticleCard";
const slug=(x:string)=>x.toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/ & /g,"-").replace(/ /g,"-");
export function generateStaticParams(){return categories.map(c=>({slug:slug(c)}))}
export default async function Category({params}:{params:Promise<{slug:string}>}){const p=await params;const title=categories.find(c=>slug(c)===p.slug)??"Actualités";let list=articles.filter(a=>a.category===title);if(!list.length)list=articles.slice(0,6);return <main><Header/><section className="category-hero"><div className="shell"><span className="section-kicker">RUBRIQUE IVOIRIENNE</span><h1>{title}</h1><p>Actualités, analyses et décisions qui comptent pour la Côte d’Ivoire.</p></div></section><section className="section shell"><div className="latest-grid">{list.map(a=><ArticleCard key={a.slug} article={a}/>)}</div><Link className="load-more" href="/explorer">Explorer toutes les publications</Link></section></main>}
