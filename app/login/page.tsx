"use client"; import Link from "next/link"; import {useRouter} from "next/navigation"; import {useState} from "react";
export default function Login(){const router=useRouter();const[email,setEmail]=useState("");const[name,setName]=useState("");const[mode,setMode]=useState<"login"|"signup">("login");function submit(e:React.FormEvent){e.preventDefault();localStorage.setItem("mock-user",JSON.stringify({name:name||email.split("@")[0],email,role:"Lecteur"}));router.push("/espace")}return <main className="auth-page"><Link href="/" className="mini-brand">L’Économiste <span>de la Côte d’Ivoire</span></Link><section className="auth-card"><span className="section-kicker">ESPACE LECTEUR · MODE DÉMO</span><h1>{mode==="login"?"Connectez-vous":"Créez votre espace"}</h1><p>Retrouvez vos favoris, préférences et lectures sur cet appareil.</p><form onSubmit={submit}>{mode==="signup"&&<label>Nom complet<input required value={name} onChange={e=>setName(e.target.value)}/></label>}<label>Adresse e-mail<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="vous@email.com"/></label><label>Mot de passe<input type="password" required minLength={6}/></label><button>{mode==="login"?"Se connecter":"Créer mon compte"}</button></form><small>Maquette locale : aucune donnée n’est transmise. <button className="text-button" onClick={()=>setMode(mode==="login"?"signup":"login")}>{mode==="login"?"Créer un compte":"J’ai déjà un compte"}</button></small></section></main>}
