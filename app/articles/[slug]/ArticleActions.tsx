"use client"; import {useState} from "react";
export function ArticleActions({slug,mode="save"}:{slug:string;mode?:"save"|"comments"}){const[saved,setSaved]=useState(false);const[comments,setComments]=useState<string[]>([]);const[text,setText]=useState("");if(mode==="save")return <button className="save-button" onClick={()=>{const n=!saved;setSaved(n);localStorage.setItem(`saved:${slug}`,String(n))}}>{saved?"★ Enregistré":"☆ Enregistrer"}</button>;return <section className="comments"><h2>Vos réactions <span>{comments.length}</span></h2><form onSubmit={e=>{e.preventDefault();if(text.trim()){setComments([...comments,text.trim()]);setText("")}}}><textarea value={text} onChange={e=>setText(e.target.value)} placeholder="Partagez votre point de vue…" required/><button>Publier</button></form>{comments.map((c,i)=><div className="comment" key={`${c}-${i}`}><span>VO</span><div><strong>Vous</strong><p>{c}</p><small>À l’instant · enregistré localement</small></div></div>)}</section>}
