import type {Metadata} from "next"; import Image from "next/image"; import Link from "next/link"; import {notFound} from "next/navigation"; import {articles,getArticle} from "../../../lib/mock-data"; import {Header} from "../../components/Header"; import {ArticleActions} from "./ArticleActions";
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const a=getArticle(slug);if(!a)return{};return{title:a.title,description:a.excerpt,alternates:{canonical:`/articles/${a.slug}`},openGraph:{type:"article",title:a.title,description:a.excerpt,images:[a.image],locale:"fr_CI",publishedTime:"2026-08-13T08:00:00+00:00",authors:[a.author]}}}
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const a=getArticle(slug);if(!a)notFound();const related=articles.filter(x=>x.slug!==a.slug).slice(0,3);return <main className="article-page"><Header/><article><header className="article-hero shell"><span className="eyebrow">{a.category} · {a.region}</span><h1>{a.title}</h1><p className="article-deck">{a.excerpt}</p><div className="article-byline"><Link href="/journaliste"><span>AK</span><div><strong>{a.author}</strong><small>Publié le {a.date} · {a.readTime}</small></div></Link><ArticleActions slug={a.slug}/></div></header><figure className="article-cover"><Image src={a.image} alt={`Illustration de l’article : ${a.title}`} fill priority sizes="100vw"/><figcaption>Image d’illustration pour la maquette éditoriale.</figcaption></figure><div className="shell article-layout"><aside className="share-rail"><span>Partager</span><a href={`https://www.facebook.com/sharer/sharer.php?u=https://leconomistedelacotedivoire.com/articles/${a.slug}`} target="_blank" rel="noreferrer">f</a><a href={`https://www.linkedin.com/sharing/share-offsite/?url=https://leconomistedelacotedivoire.com/articles/${a.slug}`} target="_blank" rel="noreferrer">in</a></aside><div className="article-body"><p className="dropcap">{a.body[0]}</p><h2>Ce qu’il faut comprendre</h2><p>{a.body[1]}</p><blockquote>Notre rédaction analyse les faits dans leur contexte ivoirien, avec une priorité donnée à l’impact sur les citoyens et les entreprises.</blockquote><h2>Les prochains enjeux</h2><p>{a.body[2]}</p><div className="article-tags"><span>{a.region}</span><span>{a.category}</span><span>Côte d’Ivoire</span></div><ArticleActions slug={`${a.slug}:comments`} mode="comments"/></div><aside className="article-aside"><h3>À lire ensuite</h3>{related.map((x,i)=><Link href={`/articles/${x.slug}`} key={x.slug}><span>0{i+1}</span>{x.title}</Link>)}</aside></div></article></main>}
