"use client"; import {useEffect,useState} from "react";
export function ReaderTools(){const[progress,setProgress]=useState(0);const[focus,setFocus]=useState(false);const[reported,setReported]=useState(false);useEffect(()=>{const onScroll=()=>{const max=document.documentElement.scrollHeight-window.innerHeight;setProgress(max?Math.min(100,Math.round(window.scrollY/max*100)):0)};window.addEventListener("scroll",onScroll,{passive:true});return()=>window.removeEventListener("scroll",onScroll)},[]);return <><div className="reading-progress" style={{width:`${progress}%`}}/><div className="reader-tools"><button onClick={()=>{setFocus(!focus);document.body.classList.toggle("focus-reading",!focus)}}>{focus?"Quitter le mode lecture":"Mode lecture"}</button><button onClick={()=>window.print()}>Imprimer</button><button onClick={()=>setReported(true)}>{reported?"Signalement reçu ✓":"Signaler une erreur"}</button></div></>}
