"use client"; import {useState} from "react";
export function SiteTools(){const[dark,setDark]=useState(false);const[alerts,setAlerts]=useState(false);const[toast,setToast]=useState("");const notify=(x:string)=>{setToast(x);setTimeout(()=>setToast(""),2200)};return <><div className="site-tools" aria-label="Préférences"><button onClick={()=>{const n=!dark;setDark(n);document.documentElement.classList.toggle("dark-mode",n)}} aria-label="Changer le thème">{dark?"☀":"☾"}</button><button className={alerts?"active":""} onClick={()=>{const n=!alerts;setAlerts(n);localStorage.setItem("eco-alerts",n?"on":"off");notify(n?"Alertes ivoiriennes activées":"Alertes désactivées")}} aria-label="Activer les alertes">♢</button></div>{toast&&<div className="site-toast" role="status">{toast}</div>}</>}
