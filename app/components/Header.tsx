"use client";
import Link from "next/link";
import { useState } from "react";
import { categories } from "../../lib/mock-data";
import { DirectFlash } from "./DirectFlash";
export function Header(){const[open,setOpen]=useState(false);const[q,setQ]=useState("");return <><div className="utility-bar"><div className="shell utility-bar__inner"><span>Le magazine économique de la Côte d’Ivoire</span><div className="utility-links"><Link href="/studio">Podcasts & vidéos</Link><Link href="/informations">Nous contacter</Link></div></div></div><header className="site-header"><div className="shell masthead"><button className="menu-button" onClick={()=>setOpen(!open)} aria-expanded={open} aria-label="Ouvrir le menu">☰</button><Link href="/" className="brand"><span className="brand__name">L’Économiste</span><span className="brand__tagline">de la Côte d’Ivoire</span></Link><form className="header-search" action="/explorer"><input name="recherche" value={q} onChange={e=>setQ(e.target.value)} placeholder="Rechercher en Côte d’Ivoire…" aria-label="Rechercher"/><button>Rechercher</button></form><div className="header-actions"><Link href="/espace" className="newsletter-button">Mon espace</Link><Link href="/login" className="login-button">Connexion</Link></div></div><nav className={`main-nav ${open?"main-nav--open":""}`}><div className="shell nav-scroll"><Link href="/">Accueil</Link>{categories.map(c=><Link key={c} href={`/categorie/${c.toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/ & /g,"-").replace(/ /g,"-")}`}>{c}</Link>)}<Link href="/regions">Régions</Link></div></nav></header><DirectFlash/></>}
