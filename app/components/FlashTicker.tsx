import {brvmQuotes} from "../../lib/mock-data";
export function FlashTicker(){const items=[...brvmQuotes,...brvmQuotes];return <section className="flash-ticker brvm-live" aria-label="Cours du jour de la BRVM"><strong className="flash-ticker__label"><i/> BRVM · ABIDJAN</strong><div className="flash-ticker__viewport"><div className="flash-ticker__track">{items.map((item,i)=><span key={`${item.symbol}-${i}`}><b>{item.symbol}</b>{item.value}<em className={`quote-${item.trend}`}>{item.change}</em></span>)}</div></div><span className="brvm-close">CLÔTURE 15:30 · MOCK</span></section>}
