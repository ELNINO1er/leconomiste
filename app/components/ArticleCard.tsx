import Image from "next/image"; import Link from "next/link"; import type {Article} from "../../lib/mock-data";
export function ArticleCard({article,large=false}:{article:Article;large?:boolean}){return <article className={`story-card ${large?"story-card--large":""}`}><Link href={`/articles/${article.slug}`} className="image-wrap"><Image src={article.image} alt="" fill sizes={large?"(max-width: 760px) 100vw, 50vw":"(max-width: 760px) 100vw, 25vw"}/></Link><div className="story-card__body"><span className="eyebrow">{article.category} · {article.region}</span><h3><Link href={`/articles/${article.slug}`}>{article.title}</Link></h3><p>{article.excerpt}</p><span className="meta">Par {article.author} · {article.readTime}</span></div></article>}
