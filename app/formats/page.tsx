import Link from "next/link"; import {Header} from "../components/Header"; import {articles} from "../../lib/mock-data";
export default function Formats(){return <main><Header/><section className="category-hero"><div className="shell"><span className="section-kicker">OPINIONS · ENTRETIENS · ENQUÊTES</span><h1>Regards sur la Côte d’Ivoire</h1><p>Des idées contradictoires, des voix fortes et des enquêtes au service du débat public.</p></div></section><section className="shell format-list">{articles.slice(0,6).map((a,i)=><article key={a.slug}><span>0{i+1}</span><div><small>{i%3===0?"OPINION":i%3===1?"ENTRETIEN":"ENQUÊTE"}</small><h2><Link href={`/articles/${a.slug}`}>{a.title}</Link></h2><p>{a.excerpt}</p><Link href={`/articles/${a.slug}`}>Découvrir →</Link></div></article>)}</section></main>}
