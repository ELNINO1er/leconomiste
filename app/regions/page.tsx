import Link from "next/link"; import {Header} from "../components/Header"; import {ArticleCard} from "../components/ArticleCard"; import {articles,regions} from "../../lib/mock-data";
export default function Regions(){return <main><Header/><section className="category-hero"><div className="shell"><span className="section-kicker">ÉCO RÉGIONS</span><h1>La Côte d’Ivoire, éco région par éco région</h1><p>Projets, entreprises, agriculture, énergie et vie publique au plus près des territoires ivoiriens.</p></div></section>{regions.map(r=>{const local=articles.filter(a=>a.region===r);if(local.length===0)return null;const list=local.slice(0,4);return <section className="section shell region-section" id={r.toLowerCase()} key={r}><div className="section-heading"><div><span className="section-kicker">ÉCO RÉGION</span><h2>{r}</h2></div><Link href={`/explorer?recherche=${encodeURIComponent(r)}`}>Explorer {r} →</Link></div><div className="latest-grid">{list.map(a=><ArticleCard key={`${r}-${a.slug}`} article={a}/>)}</div></section>})}</main>}
