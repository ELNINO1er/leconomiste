import {Header} from "../components/Header"; import {ArticleCard} from "../components/ArticleCard"; import {articles,regions} from "../../lib/mock-data";
export default function Regions(){return <main><Header/><section className="category-hero"><div className="shell"><span className="section-kicker">TERRITOIRES</span><h1>La Côte d’Ivoire, région par région</h1><p>Projets, entreprises, agriculture, culture et vie publique au plus près des territoires.</p></div></section>{regions.map(r=><section className="section shell region-section" id={r.toLowerCase()} key={r}><div className="section-heading"><div><span className="section-kicker">RÉGION</span><h2>{r}</h2></div></div><div className="latest-grid">{(articles.filter(a=>a.region===r).length?articles.filter(a=>a.region===r):articles.slice(0,3)).map(a=><ArticleCard key={`${r}-${a.slug}`} article={a}/>)}</div></section>)}</main>}
