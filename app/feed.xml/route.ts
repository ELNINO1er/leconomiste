import {articles} from "../../lib/mock-data";
const base="https://leconomistedelacotedivoire.com";
const xml=(value:string)=>value.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;");
export function GET(){const items=articles.map(a=>`<item><title>${xml(a.title)}</title><link>${base}/articles/${a.slug}</link><guid isPermaLink="true">${base}/articles/${a.slug}</guid><description>${xml(a.excerpt)}</description><category>${xml(a.category)}</category><author>${xml(a.author)}</author></item>`).join("");return new Response(`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>L’Économiste de la Côte d’Ivoire</title><link>${base}</link><description>Le quotidien économique et financier ivoirien de référence.</description><language>fr-CI</language><lastBuildDate>${new Date().toUTCString()}</lastBuildDate>${items}</channel></rss>`,{headers:{"Content-Type":"application/rss+xml; charset=utf-8","Cache-Control":"public, max-age=900"}})}
