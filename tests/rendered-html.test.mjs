import assert from "node:assert/strict"; import {readFile} from "node:fs/promises"; import test from "node:test";
test("positionne le média sur la Côte d’Ivoire",async()=>{const [home,layout,data]=await Promise.all([readFile(new URL("../app/page.tsx",import.meta.url),"utf8"),readFile(new URL("../app/layout.tsx",import.meta.url),"utf8"),readFile(new URL("../lib/mock-data.ts",import.meta.url),"utf8")]);assert.match(home,/Côte d’Ivoire/);assert.match(layout,/leconomistedelacotedivoire\.com/);assert.match(data,/San-Pédro/);assert.doesNotMatch(home,/Sélectionner un pays/)});
test("expose un SEO éditorial et des articles individuels",async()=>{const [sitemap,article]=await Promise.all([readFile(new URL("../app/sitemap.ts",import.meta.url),"utf8"),readFile(new URL("../app/articles/[slug]/page.tsx",import.meta.url),"utf8")]);assert.match(sitemap,/articles\.map/);assert.match(article,/generateMetadata/);assert.match(article,/generateStaticParams/)});
